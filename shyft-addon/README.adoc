== Homeassistant addon

This is the shyft home-assistant addon. :-)


=== Installing

* Install plugin.
* Goto github project page - https://github.com/ehmkah/ha-shyft-addon
* There is a link add add-on to home assistant
* !Attention! - its not possible to install the add-on to a homeassistant which is running from the shell (e.g. because you developed your own integration)

=== Development

* install required node packages - `npm install`
* run server `npm start`
* Run local http server for testing config
** `cd tests/`
** Start simple http server `npx json-server --watch db.json --port 8000`

* Python parts
** General information can be found in https://packaging.python.org/en/latest/guides/installing-using-pip-and-virtual-environments/[offical python documentation]

** create python venv `python3 -m venv .venv`
** open environment - `source .venv/bin/activate`
** install dependencies - `python3 -m pip install -r requirements.txt`

** After modifying a python dependency
*** `python3 -m pip freeze > requirements.txt`


=== Links

* Writing first homeassistant  addon - https://developers.home-assistant.io/docs/add-ons/tutorial/
* Homeassistant core api - https://developers.home-assistant.io/docs/api/rest/